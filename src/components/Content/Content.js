import classes from "./Content.module.css";
import PostList from "./PostList";

const Content = (props) => {
    return (
        <div>
            <PostList />
        </div>
    );
}

export default Content;